# 单仓库迁移验证（2026-09-18）

## 迁移与部署

- 五个原项目的业务源码及测试按当前工作目录复制，保留未提交修改与新增文件；复制清单中的文件无缺失。
- 根目录只有一个 Git 仓库，子目录不再包含 `.git`。
- 新增配置管理测试：6 项通过，覆盖共用令牌、不同运行模式、服务覆盖配置、密码特殊字符、重复初始化和 Compose 全局参数转发。
- 四个 Python 项目的实际配置加载器均成功加载生成的本地配置。
- 五个项目的 Docker 镜像全部构建成功。
- 在独立 Compose 项目 `testing-agent-monorepo-check` 中完成空 MySQL 初始化、Alembic 迁移和全套服务启动。
- 前端首页、前端 `/v1/health` 代理、后端 `/v1/health`、禅道 `/api/v1/health/ready` 均返回 HTTP 200。
- AI / API / UI 三个任务领取接口均收到 Worker 的已认证轮询，并对空队列返回 HTTP 204。
- 临时检查使用随机空闲宿主机端口；原本机 9000 端口已有服务。交付配置将控制面对外端口设为 19000，容器内仍使用 9000。
- 检查后清理临时部署及其独立测试数据卷。未启动持久生产部署，也未操作已有 mysql8 容器的数据。

## 原项目检查结果

| 项目 | 结果 |
| --- | --- |
| AI Worker | 161 项测试通过 |
| API/UI Worker | 34 项测试通过 |
| 禅道服务 | 62 项测试通过 |
| 控制面 | 663 通过，4 失败，29 跳过 |
| 前端 | 类型检查、Lint、生产构建通过；测试 247 通过、3 失败 |

控制面的 4 项失败均为 `test_project_membership_api.py::test_function_stage_reexecution_uses_current_actor` 的参数化用例：测试直接生成功能用例，当前业务规则要求先完成需求分析并导入增强文本，因而返回 HTTP 400。已针对原备份中的源码和测试复跑，相同 4 项失败可复现。

前端失败涉及：

- `FunctionalCaseGenerateTaskDetailPage.test.tsx`：类型按钮用例超时；优化侧栏预填用例断言失败。
- `GitlabBindings.test.tsx`：权限撤销后下拉框应禁用的断言失败。

对上述两个前端测试文件以单 Worker 复测，结果为 38 通过、2 失败：GitLab 权限用例复测通过，功能用例详情页的两项失败仍可复现，说明首次权限用例失败具有不稳定性。

前端业务源码与原备份逐文件一致。本次没有修改上述业务逻辑或测试来掩盖失败；这些测试问题作为后续业务维护事项保留。

本次冒烟检查验证部署、连通性与空队列轮询，没有调用真实模型、禅道业务实例或执行真实 UI/API 测试任务；这些场景需要平台授权、目标系统及项目 Skill 数据。

## apps 目录整理复验（2026-09-18）

五个项目移至 `apps/studio`、`apps/control-plane`、`apps/ai-worker`、`apps/api-ui-worker`、`apps/connector-zentao`。更新统一管理脚本的项目路径、Compose 构建上下文、测试临时目录创建及文档路径。

- 配置管理 6 项测试通过，Compose 配置检查通过。
- 四个 Python 虚拟环境按原锁文件在新目录重建；实际启动模块和本地配置加载验证通过。
- 五个镜像均从新构建上下文构建成功。
- 前端 `npm run verify` 通过（类型检查、Lint、生产构建）。
- 保持业务代码、依赖声明、锁文件、服务名称和运行入口不变；这次只复验目录与启动配置相关检查，未重复运行上文完整业务测试套件。
