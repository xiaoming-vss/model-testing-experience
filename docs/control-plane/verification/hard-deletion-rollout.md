# 硬删除落地验证（2026-09-17）

经用户明确授权，对本地配置指向的业务库执行一次性备份归档、孤立数据清理和迁移；这不是通用的旧库自动升级流程。

- 停服前确认没有 pending、claimed 或 running 的 Worker 任务。
- 先将完整数据库副本恢复到隔离 MySQL 库演练，再停服做最终备份。
- 业务库从 `20260916_0007` 升级到 `20260917_0008`，新增 18 个外键。
- 归档并清理 232 条软删除、孤立或所属资源失效的记录，其中包括 28 条功能用例、6 条 UI 用例；保留正常关联的正式资产。
- 备份包含 SQL、storage 文件、逐行删除归档、被清理文件及清理数量报告；最终备份目录为本机 `~/.local/state/testing-agent/backups/20260917-103445/`，目录仅当前用户可访问。
- 业务表结构与 ORM 一致；额外的 4 张既有历史归档表原样保留。业务表无剩余软删除标记，新增外键及逻辑归属检查均无孤立数据。
- 服务重启后健康检查通过。使用临时账号及临时项目，经真实 HTTP 接口验证功能/UI/API 三类测试集：有子用例时返回 409，删除用例后数据库记录实际消失，测试集可删除并同名重建。
- 临时验证数据已全部清理；业务表最终行数逐表匹配清理报告，无额外数据变更。

核心实现的完整回归为 632 项通过；隔离 MySQL 专项为 43 项通过。落地后的真实接口验证和数量对账结果保存在备份目录的 `live-smoke.json` 与 `cleanup-report.json`。

## 移除软删除字段（2026-09-17）

- 本地业务库进一步升级到 `20260917_0009`：移除 17 张业务表的 `deleted_at`、19 个相关旧索引，建立 5 个必要的业务字段索引；其余查询由已有索引覆盖。
- 删除 `SoftDeleteMixin`、全部业务查询中的软删除过滤和 `include_deleted` 参数，错误提示不再声称已删除资源占用名称。
- 使用当前业务库副本执行升级、回退、再次升级；34 张业务表的逐行内容指纹一致。正式迁移前后同样逐表验证，本次未删除或修改业务行。
- 最终数据库及文件备份位于本机 `~/.local/state/testing-agent/backups/20260917-104941/`。`column-removal-application.json` 记录数据验证，`live-smoke.json` 记录真实接口检查。
- 本地 `127.0.0.1:9000` 服务已重启，功能/UI/API 三类测试集的逐级删除、数据库物理删除、同名重建验证通过，临时测试账号及资产已清理。
- 完整回归 640 项通过；隔离 MySQL 专项 48 项通过；迁移前拒绝未清理删除标记及带数据回退测试通过。实际业务表中不再存在该字段或引用它的索引，历史归档表保存原始快照。
